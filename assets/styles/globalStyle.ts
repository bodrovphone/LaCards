import {StyleSheet, View} from 'react-native';

// should be used for design tokens like brand colors, spacing, typography, etc.
export const globalStyle = StyleSheet.create({
  primaryColor: {
    color: '#F320D2',
  },
});
